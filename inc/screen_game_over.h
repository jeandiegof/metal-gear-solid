#ifndef SCREEN_GAME_OVER_H
#define SCREEN_GAME_OVER_H

#include "inc/window.h"

#define WINDOW_GAME_OVER_LINES        32
#define WINDOW_GAME_OVER_COLUMNS      82

void ScreenGameOverInit(WINDOW **window, PANEL **panel);

#endif // SCREEN_GAME_OVER_H